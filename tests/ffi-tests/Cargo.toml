[package]
name = "ffi-tests"
version = "0.1.0"
edition = "2024"
publish = false

[[bin]]
name = "ffi-test-runner"
path = "src/main.rs"

[dependencies]
antikythera-sdk = { path = "../../antikythera-sdk", default-features = false, features = ["ffi"] }
libc = "0.2"
